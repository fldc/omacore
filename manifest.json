{
  "schemaVersion": 1,
  "id": "io.github.birajdotdev.omacore",
  "name": "Soundcore",
  "version": "0.2.0",
  "author": "Biraj",
  "license": "MIT",
  "description": "Battery, ambient sound mode and ANC settings for Soundcore headphones and earbuds in the Omarchy bar, via the OpenSCQ30 CLI.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Soundcore",
    "description": "Battery, ambient sound mode (Noise Cancelling / Transparency / Normal) with per-mode settings, plus the device's other settings.",
    "category": "Audio",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "macAddress": "",
      "model": "SoundcoreA3040",
      "pollIntervalSec": 30,
      "ctlPath": "",
      "hideWhenDisconnected": true,
      "notifyEnabled": true
    },
    "schema": [
      {
        "key": "macAddress",
        "type": "string",
        "label": "Bluetooth MAC address",
        "defaultValue": "",
        "description": "Pair the earbuds over Bluetooth first, then register them with `openscq30 paired-devices add -a <mac> -m <model>` and paste the same MAC address here, e.g. AA:BB:CC:DD:EE:FF."
      },
      {
        "key": "model",
        "type": "string",
        "label": "OpenSCQ30 model id",
        "defaultValue": "SoundcoreA3040",
        "description": "Use the model id from `openscq30 list-models` (e.g. SoundcoreA3040 = Space Q45, SoundcoreD1202C = R60i NC). Only needed for `paired-devices add`; the widget itself does not send this."
      },
      {
        "key": "pollIntervalSec",
        "type": "integer",
        "label": "Poll interval (seconds)",
        "min": 10,
        "max": 300,
        "step": 5,
        "defaultValue": 30
      },
      {
        "key": "ctlPath",
        "type": "path",
        "label": "Path to the openscq30 CLI",
        "defaultValue": "",
        "description": "Leave empty to find `openscq30` on PATH."
      },
      {
        "key": "hideWhenDisconnected",
        "type": "boolean",
        "label": "Hide the icon when the earbuds are unreachable",
        "defaultValue": true
      },
      {
        "key": "notifyEnabled",
        "type": "boolean",
        "label": "Desktop notifications",
        "defaultValue": true,
        "description": "Notify when the earbuds disconnect or a bud/case battery drops to 20% or below."
      }
    ]
  }
}
